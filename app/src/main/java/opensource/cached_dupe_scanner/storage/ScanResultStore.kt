package opensource.cached_dupe_scanner.storage

@Deprecated(
    message = "ScanResultStore is not used. Scan results are stored in Room via cached_files.",
    level = DeprecationLevel.ERROR
)
class ScanResultStore
